version: '3.8'

services:
  app:
    build: .
    container_name: edm-app
    restart: unless-stopped
    environment:
      NODE_ENV: production
      DB_DIALECT: mysql
      DB_HOST: ${DB_HOST:-212.227.165.26}
      DB_PORT: ${DB_PORT:-3306}
      DB_DATABASE: ${DB_DATABASE:-db_dlaudo_erp}
      DB_USER: ${DB_USER:-root}
      DB_PASSWORD: ${DB_PASSWORD:-O84xflL0ngKe0r}
    ports:
      - "3000:3000"
    networks:
      - edm-network

networks:
  edm-network:
    driver: bridge
